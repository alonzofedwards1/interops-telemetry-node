{
  "name": "telemetry-api",
  "version": "1.0.0",
  "description": "Minimal telemetry ingestion service for InterOps (in-memory, non-blocking)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
